import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  images: {
    domains: ["tailwindui.com","images.unsplash.com"], // Add the domain here
  },
  // Other configuration options can go here
};

export default nextConfig;
